language: csharp
dist: trusty
sudo: true

os:
  - linux
  - osx

mono:
  - nightly
  - latest
  - 5.18.0
  - 5.16.0
  - 5.14.0
  - 5.12.0

install:

script: ./mono/build-mono.sh && ./mono/test-mono.sh

allow_failures:
  - mono: nightly
